# Core analysis / EDA (runs locally on Mac, CPU)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
pyarrow>=14.0
tqdm>=4.66

# Lightweight baseline model (local, CPU)
lightgbm>=4.1

# Transformer experiments (local DeBERTa baseline on MPS, or Kaggle GPU)
torch>=2.2
transformers>=4.40
datasets>=2.18
accelerate>=0.29

# LLM fine-tuning stack — intended for Kaggle/Colab GPU, NOT local Mac.
# Uncomment there:
# peft>=0.10
# bitsandbytes>=0.43
# trl>=0.8
# sentencepiece>=0.2
